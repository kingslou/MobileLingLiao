<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cyt.ieasy.mobilelingliao" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bg"
        android:label="@string/app_name"
        android:name=".MyApplication"
        android:theme="@style/MyMaterialTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name="com.cyt.ieasy.setting.SettingActivity"
                  android:theme="@style/MaterialSettingsTheme"></activity>
        <activity android:name=".SelectDept_Stock"></activity>
        <activity android:name=".AddWuZiActivity"></activity>
        <activity android:name=".HistoryActivity"></activity>
        <activity android:name=".HistoryContent"></activity>
        <activity android:name=".MuBanActivity"
                  android:launchMode="singleTop"></activity>
        <activity android:name=".MuBanDetialActivity"></activity>
        <activity android:name=".AddotherWuZiActivity"></activity>
    </application>

</manifest>
